#!/bin/bash

# omarchy:summary=Prompt for confirmation before starting an update

gum style \
  --border double \
  --border-foreground 198 \
  --padding "1 4" \
  --margin "1 2" \
  --align center \
  "🚀 READY TO UPDATE X64 LIOS 🚀" \
  "" \
  "⚠️  WARNING: Do not interrupt the update process." \
  "⚡ Ensure your system is connected to a power source." \
  "" \
  "Preparing for system upgrade..."

echo

if ! gum confirm "Initialize Update Sequence?" --selected.background 198 --selected.foreground 232 --unselected.background 237; then
  echo
  gum style --foreground 196 " ❌ Update sequence aborted by user."
  exit 1
fi
